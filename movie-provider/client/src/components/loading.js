import React from "react";

const Loading = () => {
  return (
    <div className="loading">
      <div className="loader"></div>
      <h1 className="loading__title">Just wait whilst we pop some popcorn.</h1>
    </div>
  );
};

export default Loading;
